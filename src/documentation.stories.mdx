import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Button, Box, Image } from '@kibalabs/ui-react';

import { Toast, IToastProps, useToastManager } from '.';

<Meta
  component={Toast}
  title={'Toast'}
/>

# Toast

<br />

export const Template = (args) => {
  const toastManager = useToastManager();
  const onShowToastClicked = () => {
    toastManager.showTextToast('👋 Hello world');
  }
  return (
    <Button text='Show Toast' onClicked={onShowToastClicked} />
  );
};

<Canvas withToolbar={true} withSource={'none'}>
  <Story name='Default' args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={'Default'} />

### Examples

<Canvas>
  <Story name='Custom Toast'>
    {(args) => {
      const toastManager = useToastManager();
      const onShowToastClicked = () => {
        toastManager.showToast(<img style={{height: '3em', width: '3em'}} src='https://www.kibalabs.com/_bh20211208082922411123/assets/_generated/favicon-1024.png' />);
      }
      return (
        <Button text='Show Toast' onClicked={onShowToastClicked} />
      );
    }}
  </Story>
</Canvas>
